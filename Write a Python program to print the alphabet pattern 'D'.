def print_pattern_D():
    height = 7
    width = 4
    for i in range(height):
        if i == 0 or i == height - 1:  # First and last row
            print("*" * width)
        else:
            print("*" + " " * (width - 2) + "*")

# Example usage:
print_pattern_D()
